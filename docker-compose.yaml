version: '3'
services:
  epidocs:
    container_name: epidocs
    image: musubi42/epidocs:v0.0.3
    ports:
      - 4205:3042
    # volumes:
      # - .:/home/node/app/
    restart: unless-stopped
    networks:
      - npm_network

networks:
  npm_network:
    driver: bridge
